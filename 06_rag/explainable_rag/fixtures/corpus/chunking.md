# Chunking

Chunking 把长文档切成可检索的最小证据单元。Chunk 太大会混入多个主题并降低定位精度；Chunk 太小会丢失上下文，使答案缺少完整条件。

一个可解释的 Chunk 应保存稳定 ID、来源文件、在原文中的位置和词元数量。调试召回失败时，应先检查相关事实是否完整落入某个 Chunk，再调整最大长度或重叠策略。

段落感知切分优先保留作者原有结构。固定字符切分更简单，但可能在句子或表格中间截断信息。
