# Retrieval

BM25 是稀疏检索方法，利用词频、逆文档频率和文档长度归一化计算相关性。它对产品名、缩写、错误码等精确关键词特别有效。

TF-IDF 余弦检索把文本表示为可检查的词项权重向量。它不是神经语义嵌入，但适合作为零依赖、确定性的向量基线。

混合检索同时运行多种检索器。Reciprocal Rank Fusion（RRF）使用排名而不是原始分数进行融合，避免直接比较不同量纲的 BM25 分数和余弦相似度。
